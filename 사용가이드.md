# TodoMaster - 사용가이드

## 앱 소개

**TodoMaster**는 Flutter 기반의 최고의 일정관리 앱입니다. 직관적인 UI와 강력한 기능으로 일상의 할 일을 효율적으로 관리할 수 있습니다.

---

## 실행 방법

### 사전 요구사항
- Flutter SDK (최신 stable 버전)
- Android Studio 또는 VS Code
- Android 에뮬레이터 또는 실제 기기

### 실행 명령어

```bash
# 프로젝트 디렉토리로 이동
cd D:\flutter\todo_list

# 의존성 설치
flutter pub get

# 앱 실행
flutter run

# 특정 기기에서 실행
flutter devices        # 기기 목록 확인
flutter run -d <기기ID>
```

### 빌드

```bash
# Android APK 빌드
flutter build apk --release

# Android App Bundle 빌드
flutter build appbundle --release
```

---

## 주요 기능

### 1. 할일 관리
- **추가**: 홈 화면 우측 하단 (+) 버튼 탭
- **편집**: 할일 카드 탭
- **삭제**: 할일 카드를 왼쪽으로 스와이프
- **완료**: 체크박스 탭으로 완료/미완료 전환
- **중요 표시**: 별표(★) 아이콘 탭

### 2. 할일 상세 설정
- **제목/설명**: 할일 제목과 상세 설명 입력
- **우선순위**: 높음(빨강)/중간(주황)/낮음(초록) 선택
- **카테고리**: 업무, 개인, 쇼핑, 건강 등 카테고리 분류
- **마감일/시간**: 날짜와 시간 선택
- **반복**: 없음/매일/매주/매월 반복 설정
- **하위 작업**: 서브태스크 추가로 세부 체크리스트 관리

### 3. 필터 및 검색
- **탭 필터**: 전체 | 진행중 | 완료 | 중요 | 오늘
- **카테고리 필터**: 특정 카테고리의 할일만 표시
- **검색**: 제목/설명 텍스트 검색
- **정렬**: 최신순, 마감일순, 우선순위순, 이름순

### 4. 캘린더 뷰
- 월간 캘린더에서 마감일이 있는 날짜에 마커 표시
- 날짜 탭으로 해당 날짜의 할일 목록 확인
- 캘린더에서 직접 할일 추가 가능

### 5. 통계 대시보드
- 전체/완료/진행중 할일 수 요약
- 전체 완료율 원형 차트
- 오늘 완료/마감 카운트
- 주간 완료 추이 막대그래프
- 우선순위별/카테고리별 분포

### 6. 설정
- **다크 모드**: 라이트/다크 테마 전환
- **카테고리 관리**: 카테고리 추가/편집/삭제 (색상, 아이콘 선택)
- **데이터 초기화**: 모든 데이터 삭제

---

## 화면 구성

| 화면 | 경로 | 설명 |
|------|------|------|
| 홈 | / | 할일 목록 메인 화면 |
| 캘린더 | /calendar | 월간 캘린더 뷰 |
| 통계 | /stats | 통계 대시보드 |
| 설정 | /settings | 앱 설정 |
| 할일 추가 | /todo/add | 새 할일 생성 |
| 할일 편집 | /todo/edit/:id | 기존 할일 수정 |
| 카테고리 관리 | /categories | 카테고리 CRUD |

---

## 기술 정보

- **프레임워크**: Flutter
- **상태 관리**: Provider (ChangeNotifier)
- **라우팅**: go_router (ShellRoute 기반 하단 네비게이션)
- **로컬 저장소**: shared_preferences (JSON 직렬화)
- **캘린더**: table_calendar
- **차트**: fl_chart
- **ID 생성**: uuid

---

## 테스트

```bash
# 전체 테스트 실행
flutter test

# 코드 분석
flutter analyze
```

---

## 프로젝트 구조

```
lib/
├── main.dart              # 앱 진입점
├── app.dart               # MaterialApp / 라우터 설정
├── config/
│   ├── constants.dart     # 전역 상수, 기본 카테고리
│   ├── routes.dart        # go_router 라우트 정의
│   └── theme.dart         # Material Design 3 테마
├── models/
│   ├── category.dart      # 카테고리 모델
│   ├── enums.dart         # Priority, RepeatType 등 열거형
│   ├── subtask.dart       # 서브태스크 모델
│   └── todo.dart          # 할일 모델
├── providers/
│   ├── category_provider.dart  # 카테고리 상태 관리
│   ├── settings_provider.dart  # 설정 상태 관리
│   └── todo_provider.dart      # 할일 상태 관리
├── screens/
│   ├── calendar_screen.dart    # 캘린더 뷰
│   ├── category_screen.dart    # 카테고리 관리
│   ├── home_screen.dart        # 홈 (할일 목록)
│   ├── settings_screen.dart    # 설정
│   ├── shell_screen.dart       # 하단 네비게이션 쉘
│   ├── stats_screen.dart       # 통계 대시보드
│   └── todo_form_screen.dart   # 할일 추가/편집
├── services/
│   └── storage_service.dart    # 로컬 저장소 서비스
├── utils/
│   └── date_utils.dart         # 날짜 유틸리티
└── widgets/
    └── todo_tile.dart          # 할일 목록 아이템 위젯
```
